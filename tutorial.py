def rotateString(s, k):
# Write the function rotateString(s, k) that takes a string s 
# If k is non-negative, the function returns the string s rotated 
# k places to the left. 

# assert(rotateString('abcd',  1) == 'bcda')

	return 


# made changes 